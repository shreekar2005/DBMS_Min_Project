{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make clear && make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            // This helps VS Code understand and show compile errors
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Runs 'make' in the root project folder (toydb)."
        }
    ]
}